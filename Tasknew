# install grafana
Step 1: GPG Key download करा
sudo wget -q -O /usr/share/keyrings/grafana.key https://apt.grafana.com/gpg.key
🔹 Step 2: Repository add करा (एकाच ओळीत)
echo "deb [signed-by=/usr/share/keyrings/grafana.key] https://apt.grafana.com stable main" | sudo tee /etc/apt/sources.list.d/grafana.list
🔹 Step 3: Update करा
sudo apt update
🔹 Step 4: Grafana install करा
sudo apt install grafana -y

sudo systemctl start grafana-server
sudo systemctl enable grafana-server
===================================
# create one folder [grafana_config] and download file loki and promtail below
loki == wget https://raw.githubusercontent.com/grafana/loki/v2.8.0/cmd/loki/loki-local-config.yaml -O loki-config.yaml
promtail == wget https://raw.githubusercontent.com/grafana/loki/v2.8.0/clients/cmd/promtail/promtail-docker-config.yaml -O promtail-config.yaml

# lounch containers
1) docker run -d \
  --name loki \
  -p 3100:3100 \
  -v $(pwd)/loki-config.yaml:/etc/loki/local-config.yaml \
  grafana/loki:2.8.0 \
  -config.file=/etc/loki/local-config.yaml

2) docker run -d \
  --name promtail \
  -v $(pwd)/promtail-config.yaml:/etc/promtail/config.yaml \
  -v /var/log:/var/log \
  --link loki \
  grafana/promtail:2.8.0 \
  -config.file=/etc/promtail/config.yaml
