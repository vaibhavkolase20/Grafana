# Grafana 
# how to install grafana
      sudo apt-get install -y apt-transport-https sudo apt-get install -y software-properties-common wget
sudo wget -q -0 /usr/share/keyrings/grafana.key https://apt.grafana.com/gpg.key
